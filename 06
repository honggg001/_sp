1. 檔案管理的核心三層架構當一個程式想要讀寫檔案時，Linux 核心會透過以下三張表來協調：架構層級範圍 (Scope)核心功能與扮演角色檔案描述符表(File Descriptor Table)每個行程獨立(Per-Process)行程私有的表格。我們常看到的 fd 0 (stdin)、fd 1 (stdout)、fd 2 (stderr) 就是這張表的索引（Index），它們只負責指向全域的「開啟檔案表」。開啟檔案表(Open File Table)全域/系統級(System-Wide)記錄系統中所有目前「被打開」的實例。這裡儲存了最關鍵的資訊：檔案偏移量 (File Offset)（目前讀寫到哪裡）以及檔案狀態旗標（唯讀、唯寫等）。i-node 表(i-node Table)全域/系統級(System-Wide)對應到硬碟上真實的檔案。儲存檔案的元數據（Metadata，如檔案大小、權限、擁有者）以及指向硬碟實際資料區塊（Data Blocks）的指標。2. 當呼叫 fork() 時，檔案會發生什麼事？在你提供的 03-fork 範例中，當父行程呼叫 fork() 產生子行程時：複製檔案描述符表： 子行程會得到一張全新但內容完全複製自父行程的「檔案描述符表」。共用同一個開啟檔案表項目： 複製時，子行程的 fd 指標會指向與父行程同一個「開啟檔案表」的項目。💡 這造成的關鍵影響：共享「檔案偏移量 (Offset)」因為父子行程指向同一個開啟檔案表項目，所以它們共用同一個讀寫進度（Offset）。如果父行程先讀取了檔案的前 100 個位元組，檔案偏移量被推到 100。此時子行程接著讀取該檔案，它會從第 101 個位元組開始讀，而不是從頭（第 0 個位元組）開始。這也是為什麼在 03-fork 的程式中，父子行程同時往標準輸出（stdout，也就是 fd 1）印出文字時，它們的字不會疊在一起，而是會乖乖地接續下去。3. 對接 GitHub 範例：03-fork 與 04-fs 的差異在 03-fork 中（行程複製）： 如同圖中左側展示的，多個不同的行程（或父子行程），其私有的 fd 最終可能指向全域開啟檔案表的同一個 Entry。這時它們會共用讀寫進度。在 04-fs 中（多次呼叫 open()）：如果同一個行程（或不同行程）針對同一個檔案路徑呼叫了兩次 open()，系統會在「開啟檔案表」中建立兩個獨立的 Entry，但這兩個 Entry 最終都會指向「i-node 表」中的同一個 i-node（因為是同一個實體檔案）。這時，這兩個 fd 的讀寫進度是各自獨立、互不影響的。總結小例子：fork() 後的父與子： 不同的 fd $\rightarrow$ 同一個開啟檔案項目 $\rightarrow$ 同一個 i-node（共用 Offset）。重複 open() 同一個檔案： 不同的 fd $\rightarrow$ 不同的開啟檔案項目 $\rightarrow$ 同一個 i-node（Offset 各自獨立）。