一、檔案管理的核心三層架構

Linux核心處理檔案與行程時，主要透過以下三張表來協調：

1. 檔案描述符表 (File Descriptor Table)
- 範圍：每個行程獨立 (Per-Process)
- 角色：這是行程私有的表格。我們常看到的 fd 0、fd 1、fd 2 就是這張表的索引，它們只負責指向全域的「開啟檔案表」。

2. 開啟檔案表 (Open File Table)
- 範圍：全域/系統級 (System-Wide)
- 角色：記錄系統中所有目前被打開的檔案。這裡儲存了最關鍵的資訊：檔案偏移量 (File Offset)，也就是目前讀寫到哪裡，以及檔案狀態旗標。

3. i-node 表 (i-node Table)
- 範圍：全域/系統級 (System-Wide)
- 角色：對應到硬碟上真實的檔案。儲存檔案的元數據（如檔案大小、權限）以及指向硬碟實際資料區塊的指標。


二、當呼叫 fork() 時，檔案會發生什麼事？

在 03-fork 範例中，當父行程呼叫 fork() 產生子行程時：

1. 複製檔案描述符表：子行程會得到一張全新、但內容完全複製自父行程的檔案描述符表。
2. 共用同一個開啟檔案表項目：複製時，子行程的 fd 指標會指向與父行程同一個「開啟檔案表」的項目。

關鍵影響：共享「檔案偏移量 (Offset)」
因為父子行程指向同一個開啟檔案表項目，所以它們共用同一個讀寫進度。
- 如果父行程先讀取了檔案的前 100 個位元組，檔案偏移量被推到 100。
- 此時子行程接著讀取該檔案，它會從第 101 個位元組開始讀，而不是從頭開始。
- 這也是為什麼父子行程同時往標準輸出印出文字時，它們的字不會疊在一起，而是會順序接續下去。


三、對接 GitHub 範例：03-fork 與 04-fs 的差異

情況 A：fork() 後的父與子（對應 03-fork）
- 結構：不同的行程 fd 轉向指向同一個開啟檔案項目，再指向同一個 i-node。
- 行為：父子行程共用 Offset，讀寫進度會互相牽動。

情況 B：重複 open() 同一個檔案（對應 04-fs）
- 結構：不同的 fd 指向不同的開啟檔案項目，再指向同一個 i-node。
- 行為：兩個 fd 的 Offset 各自獨立，互不影響。例如 A 讀到第 50 位元組，B 依然會從第 0 位元組開始讀。